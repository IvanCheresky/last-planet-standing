using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface AmmunitionCollision
{
    // Start is called before the first frame update
    void SetDamage(int damage);

}
